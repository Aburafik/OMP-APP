import 'package:flutter/material.dart';

class BatterySystemVC extends StatelessWidget {
  const BatterySystemVC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Battery System"),
      ),
    );
  }
}
