import 'package:flutter/material.dart';

class InstrumentalsVC extends StatelessWidget {
  const InstrumentalsVC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("General Instrumentals"),
      ),
    );
  }
}
