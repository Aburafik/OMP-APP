import 'package:flutter/material.dart';

class PvArraysVC extends StatelessWidget {
  const PvArraysVC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),

      body: Center(
        child: Text("Pv Arrays"),
      ),
    );
}
  }
